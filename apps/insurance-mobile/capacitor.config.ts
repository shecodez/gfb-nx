import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'insurance-mobile',
  webDir: '../../dist/apps/insurance-mobile',
  bundledWebRuntime: false,
};

export default config;
